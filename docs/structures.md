# Data structure descriptions

## Dataset

dataset* dataset

* tree_ll* col_labels
    * .self=label*
* tree_ll* lines
    * .self=tree_ll* "line"
        * self=label* or double* (depends on column type)